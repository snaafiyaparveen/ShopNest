# Base URL the frontend proxies /api requests to during dev (see vite.config.js).
# In production, point this at your deployed backend URL instead.
VITE_API_BASE_URL=https://shopnest-8brc.onrender.com